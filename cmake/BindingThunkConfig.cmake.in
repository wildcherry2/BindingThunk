@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if (NOT TARGET asmjit::asmjit)
    find_dependency(asmjit CONFIG REQUIRED PATHS "${PACKAGE_PREFIX_DIR}")
endif ()

include("${CMAKE_CURRENT_LIST_DIR}/BindingThunkTargets.cmake")

check_required_components(BindingThunk)
